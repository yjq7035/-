===== A. 音频状态机（假 wx）=====
  ok   init() 报告可用  → available=true
  ok   配置正确落到实例上  → src=audio/rainfall.mp3 loop=true volume=0.6
  ok   设置了音频会话选项（静音键/混音）  → {"obeyMuteSwitch":false,"mixWithOther":false}
  ok   启动即自动播放（play 调了 1 次）  → play=1
  ok   isPlaying() 反映真实播放态  → paused=false
  ok   init() 幂等（只创建过 1 个实例）  → instances=1
  ok   toggle() 关闭并返回 false  → enabled=false
  ok   关闭后真的暂停了  → pause=1
  ok   关闭状态写进了存档（重启后不该自己响）  → {"enabled":false}
  ok   toggle() 再开启并返回 true  → enabled=true
  ok   开启后真的续播了  → play=2
  ok   切后台：暂停  → pause 1→2
  ok   回前台：自动续播（用户没关音乐）  → play 2→3
  ok   回前台：用户已关音乐 → 绝不自己复活  → play 3→3（应不变）
  ok   回前台：用户已关音乐 → 也不再多暂停一次  → pause 3→4
  ok   onError / onEnded 回调不抛异常（音乐坏了不能带崩游戏）  → ok
  ok   错误被记进状态快照（便于诊断）  → decode fail
  ok   onEnded 兜底续播（防某些环境不循环）  → play 4→5
  ok   无存档：默认开关遵循 config.MUSIC.defaultEnabled（不自动越权打开）  → defaultEnabled=false enabled=false play=0
  ok   存档记录"用户关过音乐" → 启动不自动播放  → play=0 enabled=false
  ok   自动播放被静默拦截：调了 play 却没真的响（不报错）  → play=1 playing=false
  ok   已挂上一次性触摸解锁监听  → start 监听 1 个
  ok   首次触摸补一次 play（这是静默失败唯一的兜底手段）  → play=2
  ok   兜底之后真的响了  → playing=true paused=false play次数=2 available=true enabled=true error=null
  ok   解锁后监听被摘掉（不在触摸回调里长期占位）  → start 监听 0 个
  ok   用户已关音乐 → 触摸兜底不会把它偷偷打开  → play=2→2
  ok   无 wx：init() 返回 false 且接口全都安全  → init=false available=false enabled=false
  ok   无 wx：任何接口都不抛异常（否则整条冒烟测试会挂在这）  → ok

===== B. 顶栏按钮几何（7 分辨率）=====
  ok   W=320  → music=[250,5,28,24] menu=[284,5] badge右=86
  ok   W=360  → music=[290,5,28,24] menu=[324,5] badge右=86
  ok   W=375  → music=[305,5,28,24] menu=[339,5] badge右=86
  ok   W=390  → music=[320,5,28,24] menu=[354,5] badge右=86
  ok   W=414  → music=[344,5,28,24] menu=[378,5] badge右=86
  ok   W=428  → music=[358,5,28,24] menu=[392,5] badge右=86
  ok   W=768  → music=[698,5,28,24] menu=[732,5] badge右=86

===== C. 图标渲染 & 点击 —— 断言"图标必须画在自己按钮里" =====
  ok   开启态：所有绘制都落在按钮矩形内  → 绘制 5 组，越界 0 组
  ok   开启态：图标水平居中于按钮（±2px 容差）  → {"x":320,"y":5,"w":28,"h":24}
  ok   关闭态：仍然画图标（画叉），不是一片空白  → 4 组绘制
  ok   关闭态：所有绘制都落在按钮矩形内  → 越界 0 组
  ok   战斗中真实点按 🔊：音乐被关闭  → enabled=false
  ok   战斗中真实点按 🔊 第二次：音乐被打开  → enabled=true
  ok   点顶栏关卡徽标：不误触音乐开关  → enabled=true
  ok   按下在 🔊、抬起滑到 ☰：不触发音乐切换  → enabled=true
  ok   ☰ 菜单按钮在加了 🔊 之后依然可点  → showMenu=true
  ok   战前选关：点按 🔊 能关掉音乐（音乐一进游戏就在响）  → enabled=false
  ok   战前选关：再点一次能开回来  → enabled=true
  ok   战前选关：☰ 打不开（战中菜单不该在开局就能开）  → showMenu=false
  ok   整帧渲染（含顶栏 🔊）不抛异常  → ok

===== D. 音乐开关出现在哪些场景 =====
  drawMusicButton 调用次数：battle=1  codex=0  talents=0  bag=0
  ok   战斗场景：有音乐开关  → battle=1
  ok   图签场景：当前**没有**音乐开关  → codex=0
  ok   天赋场景：当前**没有**音乐开关  → talents=0
  ok   背包场景：当前**没有**音乐开关  → bag=0

=== 音频专项汇总：失败 0 条 ===