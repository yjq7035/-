
===== 320x568 =====
  ok   战斗（战前选关） 渲染  → 指令 319 条
  ok   战斗 + 属性面板 渲染  → 指令 406 条
  ok   图签（无选中） 渲染  → 指令 370 条
  ok   图签 + 详情面板 渲染  → 指令 333 条
  ok   图签 + 选宝石浮层 渲染  → 指令 349 条
  ok   天赋 渲染  → 指令 274 条
  ok   背包 渲染  → 指令 834 条
  ok   图签：点卡片弹出详情面板  → codexSelected=triangle
  ok   图签：执行一次升级不抛异常  → ok
  ok   图签：点空白收面板  → codexSelected=null
  ok   导航第 5 格 = 背包（真实点按可进入）  → 第5格 scene=bag → game.scene=bag
  ok   背包：广告未开放时拒绝解锁（不假解锁）  → AD.enabled=false → {"ok":false,"reason":"ad"} slots 24→24
  ok   战斗：点顶栏不抛异常  → ok

===== 375x667 =====
  ok   战斗（战前选关） 渲染  → 指令 338 条
  ok   战斗 + 属性面板 渲染  → 指令 419 条
  ok   图签（无选中） 渲染  → 指令 370 条
  ok   图签 + 详情面板 渲染  → 指令 332 条
  ok   图签 + 选宝石浮层 渲染  → 指令 348 条
  ok   天赋 渲染  → 指令 334 条
  ok   背包 渲染  → 指令 834 条
  ok   图签：点卡片弹出详情面板  → codexSelected=triangle
  ok   图签：执行一次升级不抛异常  → ok
  ok   图签：点空白收面板  → codexSelected=null
  ok   导航第 5 格 = 背包（真实点按可进入）  → 第5格 scene=bag → game.scene=bag
  ok   背包：广告未开放时拒绝解锁（不假解锁）  → AD.enabled=false → {"ok":false,"reason":"ad"} slots 24→24
  ok   战斗：点顶栏不抛异常  → ok

===== 375x812 =====
  ok   战斗（战前选关） 渲染  → 指令 367 条
  ok   战斗 + 属性面板 渲染  → 指令 448 条
  ok   图签（无选中） 渲染  → 指令 370 条
  ok   图签 + 详情面板 渲染  → 指令 398 条
  ok   图签 + 选宝石浮层 渲染  → 指令 414 条
  ok   天赋 渲染  → 指令 326 条
  ok   背包 渲染  → 指令 834 条
  ok   图签：点卡片弹出详情面板  → codexSelected=triangle
  ok   图签：执行一次升级不抛异常  → ok
  ok   图签：点空白收面板  → codexSelected=null
  ok   导航第 5 格 = 背包（真实点按可进入）  → 第5格 scene=bag → game.scene=bag
  ok   背包：广告未开放时拒绝解锁（不假解锁）  → AD.enabled=false → {"ok":false,"reason":"ad"} slots 24→24
  ok   战斗：点顶栏不抛异常  → ok

===== 390x844 =====
  ok   战斗（战前选关） 渲染  → 指令 370 条
  ok   战斗 + 属性面板 渲染  → 指令 451 条
  ok   图签（无选中） 渲染  → 指令 370 条
  ok   图签 + 详情面板 渲染  → 指令 398 条
  ok   图签 + 选宝石浮层 渲染  → 指令 414 条
  ok   天赋 渲染  → 指令 326 条
  ok   背包 渲染  → 指令 834 条
  ok   图签：点卡片弹出详情面板  → codexSelected=triangle
  ok   图签：执行一次升级不抛异常  → ok
  ok   图签：点空白收面板  → codexSelected=null
  ok   导航第 5 格 = 背包（真实点按可进入）  → 第5格 scene=bag → game.scene=bag
  ok   背包：广告未开放时拒绝解锁（不假解锁）  → AD.enabled=false → {"ok":false,"reason":"ad"} slots 24→24
  ok   战斗：点顶栏不抛异常  → ok

===== 428x926 =====
  ok   战斗（战前选关） 渲染  → 指令 386 条
  ok   战斗 + 属性面板 渲染  → 指令 467 条
  ok   图签（无选中） 渲染  → 指令 370 条
  ok   图签 + 详情面板 渲染  → 指令 474 条
  ok   图签 + 选宝石浮层 渲染  → 指令 490 条
  ok   天赋 渲染  → 指令 326 条
  ok   背包 渲染  → 指令 834 条
  ok   图签：点卡片弹出详情面板  → codexSelected=triangle
  ok   图签：执行一次升级不抛异常  → ok
  ok   图签：点空白收面板  → codexSelected=null
  ok   导航第 5 格 = 背包（真实点按可进入）  → 第5格 scene=bag → game.scene=bag
  ok   背包：广告未开放时拒绝解锁（不假解锁）  → AD.enabled=false → {"ok":false,"reason":"ad"} slots 24→24
  ok   战斗：点顶栏不抛异常  → ok

=== 冒烟汇总：5 分辨率 × 7 场景，失败 0 条 ===
=== 判据戳：TOWER_ORDER=17 / 2026-09-20T06:19:03.828Z ===