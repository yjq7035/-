# 2026-09-18

## 启动崩溃：@swc/runtime/_class_call_check 未定义（MiniProgramError）
- 现象：开发者工具 v2.02.2608070 启动即崩，栈里全是 WAGameSubContext/WAGame（非本工程代码），指纹 `module '@swc/runtime/_class_call_check.js' is not defined`。
- 根因：新版微信开发者工具**默认开启 SWC 编译**，把类语法的辅助函数（_class_call_check 等）编译成 `require('@swc/runtime/...')` 外部包，小游戏包里没带 → 启动即崩。不是自己启动代码的锅。
- 修法：`project.config.json` 的 `setting` 加 `"swc": false, "disableSWC": true`（v2.02+ 必须同时两个），**完全重启**开发者工具才生效。关掉 SWC 后回落到旧 Babel 内联 helper 路径，不再外部 require。
- 注：`project.private.config.json` 的 setting 没有 es6/enhance 覆盖项，改 project.config.json 即可。

## 影音默认关闭（BGM 默认静音但开关仍能打开）
- 不能简单把 `MUSIC.enabled=false`：那会让 init() 提前 return、连音频实例都不创建，游戏内音乐开关"开不回来"。
- 正确做法：保持 `MUSIC.enabled=true`（仅作总开关/杀开关），新增 `MUSIC.defaultEnabled:false`；`audio.js` 在"无存档偏好时"用 `defaultEnabled` 作默认。
- 行为：新用户默认静音；老用户已存的开关偏好（wx storage）仍被尊重；游戏内开关可正常重新打开（实例照常创建）。
- 改了 `src/config.js`（MUSIC 加字段）、`src/audio.js`（模块级 enabled 初值 + init 偏好解析两处）。已 `node --check` 通过。

## 平行塔攻击弹道补齐（src/renderer.js）
- `drawProjectiles` 的 switch 漏了 `parallel` case → 平行塔子弹**静默隐身**（掉出 switch 什么都不画）。
- 补了 `case 'parallel'`：迷你双横（"二"字，两条 roundRect 子路径共用一次 beginPath，
  老坑照旧绕开），并且 `ctx.rotate(-(proj.angle||0))` 反向转回正立 ——
  塔本体在 NO_ROTATE_SHAPES 的规矩，弹道同样适用。
- 梯形塔（trapezoid）也漏 case，但用户确认：**梯塔是辅助光环塔，无攻击手段，永远不走弹道**，
  无需补（在 switch 里留了注释说明，防止下次有人"顺手补上"）。
- 新探针 `.workbuddy/tools/_probe-parallel-proj.js`（报告 `_parallel_proj.txt`）：
  空战场 + 手动塞一枚 parallel 弹道 → 渲染一帧 → 断言恰好 2 条 6.6×2.4 横杠 /
  左对齐 / 有缝 / 横向 / 调用了 rotate(-angle)。9 项全绿。
  ⚠️ 写弹道探针注意：全帧 UI 也画 roundRect，必须按弹道几何尺寸过滤，不能数总数。
- run-all 全量回归 10 套全绿；git status 确认 src/ 只有 renderer.js 本轮改动。
- 教训复刻：Edit 替换时把 switch 收尾括号吞了一次（old_string 尾部含 `}` 而 new_string 没有），
  Read 复核才发现 —— **替换带块级收尾括号的代码段后必须回读复核**。

## 宝石奖励结算（9 格）补全 + 修崩
- 需求：宝石只在通关/失败结算获得（过程不掉落，已在 grantKillReward/onWaveCleared 移除）；
  结算界面 9 格，每格随机 1~关卡关联数量（=GEM.gemRewardMax>0?它:关卡号）；通关概率>失败。
- 之前 `grantRewardGems` 是**坏代码**：`this.GEM.randomKind()`（this.GEM 未定义→崩）、
  `meta.addGemsByKind`/`meta.bagGemCount` 根本不存在、且结算界面没画 9 格。
- 改动：
  - `src/meta.js` 新增 `addGemsByKind(kind,count)`（背包满则能放几颗放几颗，返回 added）+ `bagGemCount()`，并导出。
  - `src/game_core.js`：constructor/restart 加 `this.gemReward=null`；`grantRewardGems(victory, level)` 重写——
    用 `GEM.victoryChance/defeatChance/rewardCellCount/gemRewardMax`，**结果写入 this.gemReward.slots（9 项 {kind,count}|null）**；
    `onLevelCleared` 先存 `clearedLevel` 再调 `grantRewardGems(true, clearedLevel)`（避免被自动推进污染关卡号）。
  - `src/renderer.js` `drawGameOver` 加 `drawGemReward`：有奖励时面板加高到 388，画 3×3 宝石格（共 X 颗 + 每格 ×N 徽标）；
    观看视频复活态不画（避免和倒计时重叠）。失败路径 line 1054 已调 `grantRewardGems(false)`。
- 验证：syntax.js 全 28 文件 ok；临时 node 脚本跑 200 次通关模拟——不抛错、slots 恒 9、触发率~86%（理论90%）、
  背包满正确走 0 颗分支。gem 总数被 bagSlots(20) 容量夹住（理论 9~45 但最多放满）。

## 宝石奖励结算 · 修正（每槽 1 颗，不是 ×N）
- 用户纠正：9 格里**每格最多 1 颗**、必为 LV1 基础宝石；奖励"总颗数"=1~min(关卡号,9)，随机点亮对应数量的格子，其余空置。之前实现成"每格 1~关卡数 颗"（×N 徽标）是错的。
- 改 `grantRewardGems`：触发后 `maxGems=min(9,关卡号)`、`gemCount=1~maxGems`、`actual=min(gemCount,背包容量)`；Fisher–Yates 洗牌从 9 格选 actual 格点亮，每格 `meta.addGemsByKind(kind,1)`（count:1）。
- `src/meta.js`：`addGem`/`addGemsByKind` 放入的宝石对象都加 `lv:1`（宝石无等级系统，固定基础宝石）。`gemList()` 返回副本会剥 lv，验证要走 addGem 返回的 gem 对象或 findGem。
- `src/renderer.js` `drawGemReward`：去掉 ×N 徽标，每格改画宝石 + 小字"Lv1"；标题加"均为 LV1 基础宝石"。
- 击杀掉落确认已移除：`grantKillReward` 不再掉宝石、`onWaveCleared` 的每 N 波 dropGem 调用已注释；`dropGem()`（game_core:705）现为**死代码**（只在 736 注释里出现），可择机删除。
- 验证：syntax 28/28 ok；node 直调真实 `Game.prototype.grantRewardGems`（绕过要 canvas 的构造函数，THEME 打桩为 global）80 次不抛错；逐字复刻算法跑 6000 次——slots 恒 9、每格 count 恒 1、点亮数===total、total∈[1,min(9,关卡)]，触发率 0.896（理论 0.9）、均值 3.5 颗。
- ⚠️ 老坑再现：`onLevelCleared` 收尾 `}` 后有一粒游离 `\`（原仓库就有，Read 显示成 `}`\），
  我的替换把它留下导致 game_core.js 编译失败，删掉后 ok。

## 宝石系统三件套：详情浮层 / 合成 / 名字带 Lv（2026-09-18 下午）
- 需求：①图签/背包点宝石弹详情浮层（图签侧底部「合成/嵌入」两项，背包侧只有「合成」）
  ②合成=同级多选，≥3 颗起成功率 50%、每多 1 颗 +10% 封顶 100%，成功产物=材料范围内**随机一种 +1 级**
  ③宝石名字带 Lv.N（背包角标 / 图签槽角标 / 浮层行名 / 面板嵌槽名 全覆盖）。
- 设计决策（用户未言明、已按此实现）：**效果随 lv 线性放大**（Lv.N = 基础效果 × N）——不然合成没意义；
  **失败材料全部消耗**（浮层页脚明示）；"同款混种"产物种类=材料范围内随机。
- 新文件 `src/gemModal.js`（三层浮层：gemPicker 嵌入列表 / gemInfo 详情 / gemSynth 合成，统一路由 hasActive/hitActive/settleTap/drawGemModal）。
  数据层 `src/meta.js`：socketed 槽位迁移为 `{kind,lv}` 对象（两代格式 normalize 兼容）、`synthesizeGems`、`synthRate`、`clampGemLv`、`embeddedEntries`。
  `src/gems.js` gemName/effectTextAt 带 Lv 缩放；`src/bag.js`、`src/codex.js`、`src/input.js`、`src/renderer.js`、`src/game_core.js` 接线。
- 探针：`probe-gem.js` 新增 ⑥ 合成数据层节 + ⑦ 真实点击端到端节（tapAt 走 input 全链路）；
  `probe-codex.js` 嵌入闭环改为"点行弹详情 → 点嵌入"两步。run-all 10 套全绿。
- ⚠️ **浮层布局随内容伸缩，点击必须"现取现点"**：合成列表 addGem 加一行 → 面板长高 → goBtn 下移 21px，
  拿旧布局引用（SL2.goBtn）去点，点的是按钮原来的位置，被模态层当 `kind:'none'` 空白吞掉——
  实测"点了没反应"全是这个。布局引用在一次改包/改选后立刻作废，重取 `getSynthLayout` 再点。
- ⚠️ probe-graphic ⑦ 的 TYPE_ALIAS 迁移断言还在用**旧字符串槽位格式**（`parallel[0]==='ruby'`），
  槽位升级为对象后必红——凡断言 socketed 内容的地方都得跟着 `{kind,lv}` 走（已改 `.kind==='ruby'`）。
- 顺手修：点"等级不符"行的 toast 原来是"至少选 3 颗"，改为按 `hit.reason` 分流（level→「同级宝石才能一起合成」）。
