# 2026-09-16

- 怪物视觉调整：移除 drawEnemy 里的"朝向鼻锥"楔形尖角（renderer.js），怪物本体回归纯圆角方块；连带清理 facing 局部变量与注释（鼻锥/朝向指示器字样）。enemy.js 中 facing 字段保留（路径切线方向，暂无渲染消费方，不影响逻辑）。node --check 通过。

- 商店槽拖放/点击顺滑度优化（自动化任务）：对比发现商店槽比放置槽"涩"的根因是——①touchstart 就 `gold<cost` 直接 return，金币不足完全拖不动；②无按压态反馈；③拖拽阈值 10px 偏大；④拖放失败无视觉反馈。修复：input.js 去掉金币门槛改允许拖拽预览、按下商店槽 pressButton、阈值 10→8、拖放失败红色 flash、resetDragState 释放按压；renderer.js drawDragPreview 重写为"抬起幽灵"（阴影+放大+半透明）并新增落点槽金/红高亮与来源槽变暗，商店槽改为按住/待拖即亮。node --check 通过，待微信实机确认按压高亮与落点提示。

- 加速怪随机刷新：补完 config.BALANCE 已预留但 wave.js 未接的 fastChance/maxFastPerWave。generateWave 改为——每只普通怪按 fastChance 概率升级为加速怪(fast,+30%速度,橙),全波加速怪数(含每3波保底1只)≤maxFastPerWave=3;第20波也按概率刷;heavy/boss 仍按 5/10 波规则。renderer.drawEnemy 给 fast 加白色»速度尖角标识。node --check 通过 + 5k×20波模拟:最快数恒≤3、每3波保底≥1、0越界、均2.37只/波。

## 属性面板重构（新增"附加绿字属性系统"）

- **新增 `src/bonusStats.js`（附加属性系统，唯一真源）**：集中收集塔的"非原生"来源，产出 原生值 base / 附加 bonus / 最终值 final / 来源明细 sources / 面板行 rows / 生效效果 effects。口径与战斗严格一致：攻击力 `floor(base × Π(1+各百分比/100))`（等级+10%/级 与 阶段增幅+100/200/400% **相乘不相加**，等于 tower.calculateFinalDamage）；攻速 `base + Σ光环点数`；攻击间隔 `原生 ÷ (最终攻速/100)`；辅助塔光环强度 `base × (1+阶段)`。扩展新来源只需 push 一条 source 记录，渲染层不用改。
- **属性面板渲染改为"基础值+加值"双色**：基础白字、加值绿字（增益）/红字（负面）。如 攻击力 `50+70`、攻速 `100%+50%`、辅助塔光环强度 `25%+50%`。等级/阶段来源在下一行小字列出（`· 等级 Lv.2  +20%`）。**渲染坑**：必须先设 `ctx.font` 再 `measureText` 再绘制，否则量宽字体与绘制字体不一致 → 双色数值重叠 2px。
- **光环纳入面板**：`AuraManager` 泛化为可携带多条 buff（正=增益/负=负面效果）+ 来源名/来源阶段/剩余时间，新增 `getAura/getAuras/getAuraValue/getBuffedValue(key)`；`applyAura` 新增第 6 参 `meta.name`，`game_core.applyTrapezoidAuras` 传入 `TOWER_DEFS[type].name`。面板新增"生效效果"小节：`● 梯塔 ★ · 攻速+50%  剩2.8s`（负值自动标红）。
- **面板高度自适应 + 切割式分隔线**：`drawTowerPanel` 重写为三步——①取附加属性系统数据 ②`wrapTextLines` 预排版并按"区块(block)"累加高度（面板高 = 内容高，封顶屏幕可用高，整体 clip 在面板内）③逐块绘制。彻底消除原来"技能槽固定 y 压在介绍文字上/文本越界"。新增 `drawTaperedDivider`：两段二次贝塞尔闭合成柳叶形，中心 5px、两端收细到 0，配横向透明度渐变；标题区改为 图标+名称+紧跟名称的阶段星/未进阶+右上角造价。
- 验证方式：mock ctx 几何断言（无重叠/无越界/未被截断，5 个场景）+ 回放成 SVG + Edge headless 截图 + canvas 像素剖面探针（中段 4 行亮、两端 0 行）。预览产物留在 `.workbuddy/preview/`（`panel_all.png` 三态总览、`tower_panel_preview.svg`）。node --check 全通过。

## 2026-09-16 自动化任务：攻击弹道大小减小50%
- src/renderer.js:1760 弹道视觉绘制尺寸 6 → 3
- src/game_core.js:752 正方塔弹道碰撞体 20 → 10

## 大版本：商店重做 + 底部导航 + 图签/天赋/关卡（元进程系统）
架构：新增"UI 原子层 + 单一真源布局函数"两层，渲染层与输入层共享同一套 getXxxLayout，杜绝两处坐标漂移。
- **`src/theme.js`（零依赖 UI 原子层）**：THEME(shade/accent.violet|cyan/surface)、12 种塔形 drawTowerIcon、drawButton/drawButtonFx/isButtonPressed、drawChip/drawPillTitle/drawDashedBox/drawLockIcon、drawTaperedDivider、roundRectPath、wrapTextLines、easeOutCubic/Back、ellipsize、pointInRect。不 require 任何业务模块（防循环依赖）。renderer 里原有的同名本地实现已全部删除改为引用本模块。
- **`src/meta.js`（元进程存档）**：wx storage + 内存兜底 + 节流保存 + normalize 坏档容错。points/talentPoints、codex 等级与加成(codexDamageMultiplier)、登场池 lineup、天赋等级/数值、关卡通关记录与所选关卡。⚠️ 顶部解构 require 必须含 `TALENT_EFFECT`（曾因文件锁 EBUSY 丢过一次，talentValue 返回 0，务必保留）。
- **`src/config.js`**：+RARITY、12 个 TOWER_DEFS（补 diamond/pentagon/oval/star，均带 attackInterval）、TOWER_ORDER、SHOP、POINTS、CODEX(maxLevel5/bonusPerLevel6/lineupMax6/unlockCost)、TALENTS(6 个，costs 数组，图鉴学起价 2)、TALENT_POINTS、TALENT_EFFECT、LEVELS(1 可玩 + 5 占位)、LAYOUT.navHeight=58。删除旧商店布局键。
- **`src/shop.js`**：getShopLayout/drawShop/hitShop/towerCost/refreshCost。卡面含稀有度/图签等级/造价/可买态/来自登场池。
- **`src/nav.js`**：NAV_ITEMS = [占位禁用, codex, battle, talents, 占位禁用]，贴屏幕最底。战斗=矢量交叉剑、天赋=矢量四角星。
- **`src/codex.js`**：3/4 列自适应格网（W<420 取 3 列）、底部详情面板(sheetH=184)、解锁(→Lv1)/升级(Lv5)/登场切换(上限 6、下限 1)。面板内部纵向节奏：名 22 / 描述 46,60 / 分隔 78 / 属性行 92(三等分) / 图签等级行 112 / 按钮顶 136。**坑**：标题区必须先设 bold 字体再 measureText 再画稀有度，否则字体宽度不一致 → 重叠。
- **`src/talents.js`**：6 天赋行 + 圆点进度 + 学习按钮（学习 ✦cost）。
- **`src/levels.js`**：关卡选择模态浮层，关卡 1 可玩，2~6 锁定"待扩展"。导出需含 getLevelBadgeRect（曾漏导致崩溃）。
- **`src/game_core.js`**：scene=battle/codex/talents、showLevels、codexSelected、toast；金币/生命并入天赋值；出货池 = meta.getLineup()；所有伤害走 towerDamage（含图签倍率）；清波/通关/失败发放积分与天赋点并落盘；主循环仅在 scene==='battle' 推进。
- **`src/tower.js`**：+getTowerStats(type)、getDiscountedCost(type,discountPercent)。**`src/bonusStats.js`**：+CODEX 来源，collectTowerStats 追加图签等级加成。
- **`src/input.js`**：全局路由优先级 = 拖拽中 → 结算 → 关卡浮层 → 导航 → 图签 → 天赋 → 战斗。
- 验证（临时脚本已删）：mock ctx 冒烟 84/84 PASS、8 分辨率布局 0 冲突、SVG 回放几何断言 7 场景 0 问题、Edge headless 出 PNG 逐张肉眼确认。预览留在 `.workbuddy/preview/v2_*.{svg,png}` + `v2_all.html`。
- 环境坑（记牢）：本机 **PowerShell 工具不回显 stdout**（连 Write-Output 都吞），**Git Bash 的 coreutils 缺失**（tail/dirname 都没有），且 **PowerShell Remove-Item 被沙箱静默拦截**。可靠做法：用 node 脚本跑测试并把输出 `fs.writeFileSync` 成 UTF-8 文件，再用 Read 读；删文件也用 node 的 fs.unlinkSync。
