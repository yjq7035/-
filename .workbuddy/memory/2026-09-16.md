# 2026-09-16 工作日志

## 插屏广告 API 对齐官方文档
- 按 InterstitialAd 官方文档改 src/ads.js：init 时预加载插屏；onClose 后自动重新预加载；onError 后按 30s 间隔重试（不再无限快刷）；show 失败时 load→show 兜底一次；增加 60s 频控与 wx.createInterstitialAd 存在性检查。
- src/config.js 的 AD 新增 interstitialMinIntervalSec=60、interstitialRetrySec=30。
- 语法校验通过；showInterstitial 仅有 ads.js 内定义，暂无外部调用点。

## 修复 errCode 1002（广告位 ID 占位符崩溃）
- 真机报 SystemError errCode 1002「广告单元长度异常」：config.js 里 adunit 仍是占位符（'adunit-interstitial' 等）。
- ads.js 新增 _isValidAdUnitId 校验（^adunit-[0-9a-z]{16,}$），激励/插屏/Banner 创建前先校验；真实 ID 格式为 'adunit-' + 16 位十六进制。
- 新增 _createMockAd 模拟广告（AD.mock=true 时启用）：用 wx.showModal 模拟广告流程，跑通 load/show/onClose(isEnded) 链路，方便无真实广告位时开发测试。上线前须把 AD.mock 改 false 并填真实 ID。
- 待办：到 mp.weixin.qq.com 流量主后台申请真实广告位（需满足用户量门槛约 1000）后替换 config.js 的三个 adunit 并关闭 mock。
