1) 构造后 ctx.roundRect 类型 = function   （兜底已生效）

2) 现在把兜底摘掉，精确还原"没有 polyfill"的旧环境：
   ctx.roundRect 类型 = undefined

3) 直接画一帧，看会不会抛异常：
   ★ 抛异常：TypeError: ctx.roundRect is not a function
     TypeError: ctx.roundRect is not a function
     at drawSlots (F:\图形塔防\src\renderer.js:143:9)
     at drawBattle (F:\图形塔防\src\renderer.js:1507:3)
     at Object.render (F:\图形塔防\src\renderer.js:1759:5)
     at Game.draw (F:\图形塔防\src\game_core.js:1618:14)
     at Object.<anonymous> (F:\图形塔防\.workbuddy\tools\noroundrect.js:54:5)

4) 重新跑真实入口的启动场景（战前选关），验证"有兜底"时不会炸：
   有兜底 → 正常渲染，无异常

结论：ctx.roundRect 缺失时，renderer 的首帧 draw() 直接抛 TypeError；
      没有 try/catch 的主循环会因此永久停摆 —— 就是"整个界面点不动"。
