=== 反证：重试启动时 wx.createCanvas 调用了几次 ===

1) 当前 game.js（带幂等缓存）：
   createCanvas 调用次数 = 1（canvas#1）
   实际 new Game 次数 = 2，第二次拿到的画布 = canvas#1
  ok   当前版本：重试没有重新创建画布（createCanvas 只调 1 次）   [实际 1 次]
  ok   当前版本：第 2 次 new Game 拿到的仍是屏幕画布 canvas#1   [canvas#1]

2) 反证版（摘掉 if (!canvas) 缓存，等价于旧写法）：  补丁是否生效 = true
   createCanvas 调用次数 = 2（canvas#1, canvas#2）
   第 2 次 new Game 拿到的画布 = canvas#2
  ok   反证成立：没有幂等缓存时，重试会创建第 2 块画布   [实际 2 次]
  ok   反证成立：第 2 次拿到的是**离屏画布** canvas#2（画面不会显示）   [canvas#2]

结论：miniprogram 的 wx.createCanvas() 只有首次返回屏幕画布；
     重试启动必须复用同一块 canvas，否则重试成功后画在离屏画布上 ——
     表现同样是「整个界面点不动」。

============================================
  通过 4   失败 0
============================================
